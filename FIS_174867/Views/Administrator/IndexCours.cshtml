@model IEnumerable<FIS_174867.Models.Cours>

@{
    ViewBag.Title = "IndexCours";
    Layout = "~/Views/Shared/Adminlayout.cshtml";
}

<h2>Display Course</h2>

@using (Html.BeginForm())
{
    <h3>
        <input type="text" name="div" />
        <input type="submit" value="Filter With Course Name" class="btn btn-info" />
    </h3>
}
<p>
    @Html.ActionLink("Create New", "CreateCours")
</p>
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.CourseID)*@
            Course ID
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.CourseName)*@
            Course Name
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.CourseCredits)*@
            Course Credits
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Department.DeptName)*@
            Department Name
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.CoursesTaught.CourseID)
        </th>*@
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CourseID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseCredits)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DeptName)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.CoursesTaught.CourseID)
        </td>*@
        <td>
            @Html.ActionLink("Edit", "EditCours", new { id = item.CourseID }) |

            @Html.ActionLink("Delete", "DeleteCours", new { id = item.CourseID })
        </td>
    </tr>
    }

</table>
