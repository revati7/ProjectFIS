@model IEnumerable<FIS_174867.Models.Publication>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/facultylayout.cshtml";
}

<h2>Publication</h2>

@*@using (Html.BeginForm())
{
    <h3>
        <input type="text" name="div" />
        <input type="submit" value="Filter With Article name" class="btn btn-info" />
    </h3>
}*@
<p>
    @Html.ActionLink("Create New", "CreatePublication")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Faculty.FacultyID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Faculty.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicationTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ArticleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublisherName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicationLocation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CitationDate)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Faculty.FacultyID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Faculty.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublicationTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ArticleName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublisherName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PublicationLocation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CitationDate)
            </td>

            <td>
                @Html.ActionLink("Edit", "EditPublication", new { id = item.PublicationID }) |
                @*@Html.ActionLink("Details", "Details", new { id=item.PublicationID }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.PublicationID })*@
            </td>
        </tr>
    }

</table>

</table>
